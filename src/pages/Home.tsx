import styled from "styled-components";

const Home = () => {
  return (
    <HomeContainer>
      <h1> HOME</h1>
    </HomeContainer>
  );
};

const HomeContainer = styled.div`
  height: 70vh;
  margin: auto;
  color: white;
  font-size: 60px;
`;

export default Home;
